for t in range(int(input())):
    a,b=map(int,input().split())
    x=min(a,b)
    y=max(a,b)
    l=[]
    for i in range(y):
        x=x*2
        l.append(x)
    if a==b:
        print("YES")
    elif y in l:
        print("YES")
    else:
        print("NO")
